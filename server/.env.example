# Comma-separated browser origins allowed to call the API in production.
# Keep production origins in deployment environment configuration, not source code.
FRONTEND_ORIGINS=https://your-frontend.example.com
