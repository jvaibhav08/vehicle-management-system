import type { Metadata } from "next";
import { BarChart3, Car, FileCheck, ShieldCheck } from "lucide-react";
import { FeatureCards } from "@/components/Content";
export const metadata: Metadata = { title: "Features", description: "See how VMS keeps vehicle, insurance and PUC records organized.", alternates: { canonical: "/features" } };
export default function FeaturesPage(){return <main><section className="page-hero"><div className="shell"><h1>Fleet management, made clearer.</h1><p>Purpose-built tools for the essential records behind every reliable fleet.</p></div></section><section className="section"><div className="shell"><FeatureCards/><div className="grid-3" style={{marginTop:20}}>{[[Car,"Vehicle management","Store the core details of every vehicle in a clear, consistent register."],[ShieldCheck,"Insurance tracking","Understand coverage status and prioritize upcoming renewals."],[FileCheck,"PUC records","Keep certification details and expiry status close at hand."],[BarChart3,"Reports","Turn daily records into useful operational visibility."]].map(([Icon,title,text])=>{const I=Icon as typeof Car;return <article className="card" key={String(title)}><div className="icon-box"><I size={21}/></div><h3>{String(title)}</h3><p>{String(text)}</p></article>})}</div></div></section></main>}
