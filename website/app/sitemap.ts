import type { MetadataRoute } from "next";import { getPosts } from "@/sanity/lib/queries";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://yourdomain.com";const pages=["","/features","/about","/blog","/contact","/privacy-policy","/terms"].map((path)=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:path===""?1:0.7}));const posts=await getPosts();return [...pages,...posts.map((post:any)=>({url:`${base}/blog/${post.slug}`,lastModified:post.publishedAt?new Date(post.publishedAt):new Date(),changeFrequency:"monthly" as const,priority:0.6}))]}
